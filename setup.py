from setuptools import setup, find_packages

setup(
    name='gerry',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Any required packages
    ],
    # Other metadata
)
